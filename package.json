{
  "name": "ui",
  "license": "MIT",
  "dependencies": {},
  "engines": {
    "node": ">=20"
  },
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "fix": "biome check . --write",
    "format": "biome format . --write",
    "prepublishOnly": "turbo run build --filter=@theanalog/orphos",
    "release": "changeset version && changeset publish"
  },
  "sideEffects": false,
  "type": "module",
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@changesets/cli": "^2.27.10",
    "@total-typescript/tsconfig": "^1.0.4",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "workspaces": ["packages/orphos", "packages/playground"],
  "packageManager": "bun@1.1.38"
}
